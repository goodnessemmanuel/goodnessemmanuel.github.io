domain: goodnessemmanuel.github.io/public
url: https://goodnessemmanuel.github.io # the base hostname & protocol for your site, e.g. http://hexad.de
baseurl: goodnessemmanuel.github.io/public # the subpath of your site, e.g. /blog
title: "" # the name of your site, e.g. ACME Corp.

kramdown:
  syntax_highlighter: rouge
  input: GFM

collections:
  expertise:
    output: false
  expertisecontent:
    output: true
  clients:
    output: false
  works:
    output: false
  socialcommitments:
    output: false

exclude:
  [nbproject, yarn.lock, package.json, package-lock.json, gitlab-ci.yml, public]
